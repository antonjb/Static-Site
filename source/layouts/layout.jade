- var title = project.courseTitle + (project.assetTitle ? ' - ' + project.assetTitle : '')
include ../includes/mixins
html
    head
        title= title
        +stylesheet('shared/css/style.css')
        block styles
    body
        include ../includes/nav
        block content
        include ../includes/footer

    block scripts